//
// Created by Semen on 9/6/2018.
//

#include "BigInteger.h"


BigInteger &BigInteger::operator+(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::operator-(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::operator*(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::operator/(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::operator%(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::to_power(int power) {
    return <#initializer#>;
}

bool operator<(const BigInteger &left, const BigInteger &right) {
    return false;
}

bool operator>(const BigInteger &left, const BigInteger &right) {
    return false;
}

bool operator==(const BigInteger &left, const BigInteger &right) {
    return false;
}

BigInteger &BigInteger::modulo_addition(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::modulo_subtractions(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::modulo_multiplication(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::modulo_division(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::modulo_remainder(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::modulo_to_power(const BigInteger &other) const {
    return <#initializer#>;
}

BigInteger &BigInteger::root_quotient(const BigInteger &other) const {
    return <#initializer#>;
}
